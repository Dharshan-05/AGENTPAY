# TOP 10 HIGH-VALUE UI RECOMMENDATIONS (FOR STEP 2 REMEDIATION)

1. **Standardize Table Scrollbars**: Add `scrollbar-thin scrollbar-thumb-slate-800 scrollbar-track-slate-950` to all table wrapper containers across 104 routes.
2. **Normalize Table Column Header Typography**: Standardize header cell typography across all tables to `text-[10px] font-mono text-slate-500 uppercase tracking-wider`.
3. **Unify Filter Control Input Heights**: Ensure search inputs and select dropdowns maintain an exact height of `py-2 text-xs font-mono`.
4. **Standardize Status Badge Font Weights**: Enforce `font-bold font-mono text-[10px]` on all `AGBadge` and inline status tags.
5. **Align Page Header Spacing**: Verify all pages use `space-y-6` for top-level page content wrappers.
6. **Harmonize Row Hover Backgrounds**: Ensure all table data rows use `hover:bg-slate-900/40 transition-colors`.
7. **Unify Drawer Inspector Footers**: Standardize `AGDrawer` footer action button container to `space-y-2 p-4 border-t border-white/[0.08] bg-slate-950`.
8. **Normalize Monospace Identifier Colors**: Standardize primary IDs (`TXN-AGP-*`, `AGT-*`, `CUS-AGP-*`) to `text-blue-400 font-bold font-mono`.
9. **Unify Action Button Icon Sizes**: Enforce `w-3.5 h-3.5 mr-1.5` on icon buttons across `PageHeader` actions.
10. **Refine Mobile Grid Gap Consistency**: Ensure responsive KPI grids use `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4` universally.
