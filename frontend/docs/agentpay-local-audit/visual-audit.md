# AGENTPAY VISUAL DESIGN AUDIT

## Master Obsidian Dark System Adherence Score: 98/100

### Background & Surfaces
- Root background `#020617` enforced across all layouts via `bg-slate-950`.
- Secondary surfaces `#050816` implemented via translucent glass cards (`bg-slate-900/60`, `backdrop-blur-xl`).
- Glass borders: `border border-white/[0.08]` providing crisp enterprise separation.

### Color Accents & Status Indicators
- **Emerald (`text-emerald-400`, `bg-emerald-500/10`)**: Healthy engine state, active status, high auth success rates.
- **Blue (`text-blue-400`, `bg-blue-500/10`)**: Primary navigation badges, intent IDs, agent references.
- **Purple / Violet (`text-purple-400`)**: AI policy identifiers, network tokens, cryptographic general ledger entries.
- **Amber (`text-amber-400`)**: Warnings, HITL manual review queues, pending approvals.
- **Rose / Red (`text-rose-400`)**: Critical risk scores (70–100), blocked transactions, open chargeback disputes.

### Typography & Spacing
- Monospace font (`font-mono`) enforced for IDs (`PM-AGP-001`, `TXN-AGP-91F2`, `CUS-AGP-001`), hashes, amounts, and metadata.
- Space Grotesk (`font-display`) for high-level section titles.
- High information density designed for financial operations personnel.
