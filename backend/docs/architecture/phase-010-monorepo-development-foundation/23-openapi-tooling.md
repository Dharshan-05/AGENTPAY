# AGENTPAY — 23: OpenAPI Generation & Spectral Linting Workflows

## 1. OpenAPI Tooling Commands

* `pnpm openapi:generate`: Generates `openapi.json` from `@agentpay/api-contracts` Zod schemas.
* `pnpm openapi:lint`: Validates OpenAPI specifications using `@stoplight/spectral-cli`.
