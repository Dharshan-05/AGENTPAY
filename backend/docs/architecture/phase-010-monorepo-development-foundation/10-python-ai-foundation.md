# AGENTPAY — 10: Python AI Microservice Setup (`poetry` / `pipenv`)

## 1. AI Agent Runtime Stack

* **Python Version**: `Python 3.11+`
* **Framework**: FastAPI + Uvicorn
* **ML / Agent Framework**: XGBoost (Risk Model), LangChain / OpenAI SDK (Agent reasoning loops)
* **Dependency Isolation**: Virtualenv / Poetry in `apps/agent-runtime` and `packages/risk`
