# AGENTPAY — 28: Razorpay Test Mode & Sandbox Mock Provider Setup

## 1. Sandbox Adapter Integration

* Development environment strictly utilizes Razorpay Test Mode keys (`rzp_test_*`).
* Exposes mock payment provider adapter for offline integration tests (`MockPaymentAdapter`).
