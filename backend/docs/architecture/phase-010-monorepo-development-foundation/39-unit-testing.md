# AGENTPAY — 39: Vitest / PyTest Unit Test Infrastructure

## 1. Unit Testing Rules

* Fast, in-memory test suites using `Vitest` for TypeScript and `pytest` for Python microservices.
* Mandatory unit test coverage on financial calculation helpers, state transitions, Zod schemas, and policy evaluation rules.
